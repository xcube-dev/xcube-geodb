name: xcube_geodb
channels:
  - conda-forge

dependencies:
  - python >=3.10
  - geopandas
  - pip
  - psycopg2
  - python-dotenv
  - requests
  - requests-mock
  - shapely
  # testing:
  - ipyleaflet
  - matplotlib-base
  - pip:
      - testing.postgresql  # not available from conda