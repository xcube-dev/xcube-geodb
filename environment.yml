name: xcube_geodb
channels:
  - conda-forge

dependencies:
  - python >=3.9
  - ipyleaflet
  - geopandas
  - matplotlib-base
  - pip
  - psycopg2
  - python-dotenv
  - requests
  - requests-mock
  - shapely
  - pip:
      - testing.postgresql  # not available from conda