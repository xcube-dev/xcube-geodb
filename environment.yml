name: xcube_geodb
channels:
  - conda-forge

dependencies:
  # Python
  - python >=3.6
  # Required
  - pip
  - geopandas
  - matplotlib-base
  - descartes
  - requests
  - psycopg2
  - python-dotenv
  - pip:
      - fastjsonschema
      - gsconfig-py3
      - testing.postgresql
      - requests_mock
      - ipyauth
      - pgsanity
  # Exploring
  - jupyterlab
  # Testing
  - pytest >=4.4
  - pytest-cov >=2.6
  - httpretty



