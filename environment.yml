name: xcube_geodb
channels:
  - conda-forge

dependencies:
  - python >=3.6
  - pip
  - geopandas
  - matplotlib-base
  - psycopg2
  - requests
  - ipyleaflet
  - python-dotenv
  - requests-mock
  - shapely
  - pip:
      - testing.postgresql  # not available from conda