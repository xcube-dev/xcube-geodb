name: dcfs-geodb
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.6
  # Required
  - jupyterlab
  - geopandas >=0.5
  - matplotlib-base
  - descartes
  - requests
  # Testing
  - pytest >=4.4
  - pytest-cov >=2.6

